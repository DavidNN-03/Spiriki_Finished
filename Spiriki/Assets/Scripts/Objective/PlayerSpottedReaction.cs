namespace Spiriki.Objective
{
    public enum PlayerSpottedReaction /*What should happen, if the player is spotted.*/
    {
        ReloadScene, /*Reload the current scene*/
        LoadScene, /*Load the scene with a given build-index.*/
    }
}